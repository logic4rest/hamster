# 11차시 — 영상처리 준비

> 출처: [파이썬으로 배우는 햄스터와 인공지능 (ver240619)](파이썬으로-배우는-햄스터와-인공지능_ver240619.pdf) PDF 180–188쪽

이후 영상 처리(사물검출, 얼굴 검출, 손모양 인식 등) 차시를 위한 준비 단계로, `roboidai`·`mediapipe` 라이브러리를 설치하고 카메라 화면을 불러오며, 여러 동작을 동시에 수행하는 병렬 처리와 이벤트 기반 프로그래밍을 배운다.

---

## 01 라이브러리 설치하기(roboid ai) (181–182p)

1. VS Code에서 New File을 열어준다. 예제 코드를 작성 후 F5를 눌러 실행시킨 다음 디버그 구성에서 Python 파일을 선택한다.
2. 실행 결과, 아래에 Terminal 창에 결과가 출력된다. (헷갈리면 1차시 라이브러리 설치 항목을 참고)
3. 터미널 창에 다음을 입력하여 `roboidai` 라이브러리와 `mediapipe` 라이브러리를 설치한다. (하이픈(-) 1개, 대문자 U)
   ```
   pip install -U roboidai mediapipe
   ```

`roboidai`와 `mediapipe`를 함께 설치하는 이유: mediapipe는 인체를 대상으로 하는 비전 인식 기능들을 AI 모델 개발과 기계학습까지 마친 상태로 제공되는 서비스라, 다양한 프로그램언어에서 사용하기 편하게 라이브러리 형태로 모듈화되어 제공되어 AI 기능을 활용한 응용 프로그램 개발이 가능하다.

---

## 02 카메라 화면 불러오기 (183p)

`ai.Camera`를 이용하여 카메라 화면을 불러온다.

**Camera 내부 인자**
- `id`: 카메라 종류 — USB 카메라 → `usb0`, `usb1`... / IP 카메라 → `ip0`, `ip1`...
- `flip`: 카메라 화면 출력 형태 — `'h'`: 좌우 대칭, `'v'`: 원점 대칭, `'a'`: 상하 대칭

```python
import roboidai as ai  # roboidai 라이브러리를 ai라고 불러옵니다.

cam = ai.Camera('usb0')  # ai의 기능 중 하나인 Camera를 불러와 내장 카메라('usb0')를 cam으로 저장합니다.

while True:
    image = cam.read()  # 이미지를 1장씩 읽습니다.

    cam.show(image)  # 읽은 이미지를 창에 띄웁니다.
    # 이미지를 1장씩 읽고 띄우지만, while문을 통해 행위를 반복하여 영상으로 띄워줍니다.
    if cam.check_key() == 'esc': break  # esc키가 입력 시, 프로그램이 종료됩니다.
```

---

## 03 병렬처리 및 이벤트 (184–188p)

### 문제 상황 — 순차 실행의 한계

아래와 같이 음악을 연주하는 동안 다른 일을 할 수 없다.
```python
for _ in range(2):
    hamster.note("C4", 0.5)
    hamster.note("E4", 0.5)
    hamster.note("G4", 0.5)

for _ in range(3):
    hamster.note("A4", 0.5)

hamster.note("G4", 1)
hamster.note("OFF", 0.5)
```

마찬가지로 아래와 같이 작성하면 햄스터의 LED를 조정하는 동안 다른 일을 할 수 없다.
```python
for _ in range(5):
    hamster.leds("RED", "RED")
    wait(500)
    hamster.leds("OFF", "OFF")
    wait(500)
```

이 두 가지 일을 동시에 하려면 어떻게 해야 할까? `parallel` 함수를 사용하면 여러 가지 일을 동시에 할 수 있다.

### 병렬 처리 — parallel()

병렬 처리는 연산 처리의 성능을 개선하기 위해 이용한다.

```python
from roboid import *

hamster = HamsterS()

def play_music():
    for _ in range(2):
        hamster.note("C4", 0.5)
        hamster.note("E4", 0.5)
        hamster.note("G4", 0.5)
    for _ in range(3):
        hamster.note("A4", 0.5)
    hamster.note("G4", 1)
    hamster.note(0, 0.5)

def blink():
    for _ in range(5):
        hamster.leds("RED", "RED")
        wait(500)
        hamster.leds("OFF", "OFF")
        wait(500)

parallel(play_music(), blink())
```

**손을 막으면 뒤로 이동하기**
```python
from roboid import *

hamster = HamsterS()

while True:
    if hamster.left_proximity() > 30 or hamster.right_proximity() > 30:
        hamster.wheels(-30)  # 뒤로 이동한다.
        wait(1000)  # 1초
        hamster.stop()  # 정지한다.
    wait(20)  # 너무 빨리 반복하지 않도록 한다.
```

**검은색 선을 만나면 삐 소리 내기**
```python
from roboid import *

hamster = HamsterS()

while True:
    if hamster.left_floor() < 20 or hamster.right_floor() < 20:
        hamster.beep()

    wait(20)  # 너무 빨리 반복하지 않도록 한다.
```

### 이벤트 처리 — when_do() / wait_until()

각 조건에 해당하는 상황이 발생하는 것을 이벤트라고 한다. 우선 각각의 상황을 판단하는 함수와 각 상황에 대한 동작을 수행하는 함수를 만든다. 이 때, 상황을 판단하는 함수는 True 또는 False를 반환해야 한다.

**when_do() 방식**
```python
from roboid import *

hamster = HamsterS()

def when_hand_found():  # 손이 감지되었을 때(양쪽 근접 센서 값 중 1개라도 30이 넘을 때)의 이벤트
    return hamster.left_proximity() > 30 or hamster.right_proximity() > 30

def do_move_backward():  # 후진 후 1초 있다 정지의 이벤트
    hamster.wheels(-30)
    wait(1000)
    hamster.stop()

def when_on_black_line():  # 검은 선 위에 있을 때(양쪽 바닥 센서 값 중 1개라도 20보다 작을 때)
    return hamster.left_floor() < 20 or hamster.right_floor() < 20

def do_sound():  # 햄스터가 삐 소리
    hamster.beep()

when_do(when_hand_found, do_move_backward)
when_do(when_on_black_line, do_sound)

wait(-1)  # Ctrl+C를 누를 때까지 계속 기다립니다.
```

`while_do()` — 조건 함수 이름을 `while_`로 시작하도록 관례를 바꿔도 동일하게 동작한다.
```python
from roboid import *

hamster = HamsterS()

def while_hand_found():  # 손이 감지되었을 때(양쪽 근접 센서 값 중 1개라도 30이 넘을 때)의 이벤트
    return hamster.left_proximity() > 30 or hamster.right_proximity() > 30

def do_move_backward():  # 후진 후 1초 있다 정지의 이벤트
    hamster.wheels(-30)
    wait(1000)
    hamster.stop()

def when_on_black_line():  # 검은 선 위에 있을 때(양쪽 바닥 센서 값 중 1개라도 20보다 작을 때)
    return hamster.left_floor() < 20 or hamster.right_floor() < 20

def do_sound():  # 햄스터가 삐 소리
    hamster.beep()

while_do(while_hand_found, do_move_backward)
when_do(when_on_black_line, do_sound)

wait(-1)  # Ctrl+C를 누를 때까지 계속 기다립니다.
```

**wait_until() 방식** — 특정 조건이 참이 될 때까지 기다렸다가 다음 코드를 실행한다.
```python
from roboid import *

hamster = HamsterS()

def hand_found():  # 손이 감지되었을 때(양쪽 근접 센서 값 중 1개라도 30이 넘을 때)의 이벤트
    return hamster.left_proximity() > 30 or hamster.right_proximity() > 30

wait_until(hand_found)
hamster.wheels(-30)
wait(1000)
hamster.stop()
```
